# Lancamento de um dado
S <- 1:6 # Espaço amostral
E <- c(2, 4, 6) # Evento: sair numero par

length(E) / length(S) # Probabilidade de sair um numero par

E <- 3 # Evento: sair 3
length(E) / length(S) # Probabilidade sair 3

E <- 1:2 # probabilidade de sair um numero menor que 3
length(E) / length(S)
